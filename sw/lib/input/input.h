#include "inc/Blynk.h"

/**
 * @brief Initializes blynk to be set up on
 * port E and uses Timer2A to periodically 
 * fill up a queue with data from blynk every
 * 10 ms
 * 
 */
void Input_Init(void);